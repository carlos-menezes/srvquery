{
  "name": "@srvquery/game-dayz",
  "version": "0.0.0",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "rolldown -c rolldown.config.ts && tsc -p tsconfig.json",
    "prepublishOnly": "pnpm build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@srvquery/core": "workspace:*",
    "@srvquery/protocol-valve": "workspace:*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@internal/typescript-config": "workspace:*",
    "@types/node": "^26.4.1",
    "rolldown": "catalog:tooling",
    "typescript": "catalog:",
    "vitest": "^5.0.0"
  }
}
